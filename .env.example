# ==============================================================================
# Core Configuration
# ==============================================================================

# Full URL to your Tautulli instance
# Use container name if on same Docker network, or IP address/hostname if different
TAUTULLI_URL=http://tautulli:8181

# Your Tautulli API key
# Found in: Tautulli → Settings → Web Interface → API
TAUTULLI_API_KEY=your-tautulli-api-key-here

# Number of days to look back for recently added media
DAYS_BACK=7

# CRON expression for scheduled execution
# Examples: "0 9 * * *" (daily at 9 AM), "0 16 * * SUN" (Sundays at 4 PM)
# Not required if RUN_ONCE=true
CRON_SCHEDULE=0 16 * * SUN

# ==============================================================================
# Discord Notifications (Optional)
# ==============================================================================

# Discord webhook URL to send release summaries
# To get a webhook: Discord Server Settings → Integrations → Webhooks → New Webhook
# Leave unset or commented out to disable Discord notifications
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Plex server URL for creating clickable links in Discord
# Default (https://app.plex.tv) works for most users - auto-detected Plex links work out of the box!
# Only change if you want direct local links instead of Plex.tv links
# PLEX_URL=https://app.plex.tv

# Plex server machine identifier (auto-detected from Tautulli)
# You don't need to set this - it's automatically fetched from Tautulli on startup
# Only set manually if auto-detection fails
# PLEX_SERVER_ID=your-plex-server-machine-identifier

# ==============================================================================
# Execution Mode
# ==============================================================================

# Set to "true" for one-shot execution (runs once and exits)
# When false or unset, runs as daemon with CRON_SCHEDULE
# RUN_ONCE=true

# ==============================================================================
# Advanced Settings
# ==============================================================================

# Logging verbosity: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Container timezone (affects log timestamps and CRON schedule interpretation)
# Examples: Europe/Paris, America/New_York, UTC
TZ=Europe/Paris

# Override default batch size for fetching items from Tautulli
# Auto-determined by DAYS_BACK: 100 (1-7 days), 200 (8-30 days), 500 (31+ days)
# Only set if you have a very large library or slow network connection
# INITIAL_BATCH_SIZE=100
