# Tautulli Configuration
TAUTULLI_URL=http://tautulli:8181
TAUTULLI_API_KEY=your-tautulli-api-key-here

# Application Settings
DAYS_BACK=7
LOG_LEVEL=INFO
TZ=Europe/Paris

# Discord Notification (Optional)
# DISCORD_WEBHOOK_URL: Discord webhook URL to send release summaries
# To get a webhook URL: Discord Server Settings > Integrations > Webhooks > New Webhook
# Leave unset or comment out to disable Discord notifications
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Plex Configuration for Discord Links (Optional)
# PLEX_URL: URL to your Plex server (default: https://app.plex.tv for Plex.tv access)
# Use https://app.plex.tv for remote access or http://your-plex-server:32400 for local
# PLEX_URL=https://app.plex.tv

# PLEX_SERVER_ID: Your Plex server's machine identifier (auto-detected if not set)
# Usually you don't need to set this - it will be automatically fetched from Tautulli.
# Only set this manually if auto-detection fails or you want to override it.
# PLEX_SERVER_ID=your-plex-server-machine-identifier

# Performance Tuning (Optional)
# INITIAL_BATCH_SIZE: Override the default batch size for fetching items from Tautulli.
# By default, batch size is determined by DAYS_BACK:
#   - 1-7 days: 100 items per batch
#   - 8-30 days: 200 items per batch
#   - 31+ days: 500 items per batch
# Set this if you want to use a custom batch size (useful for large libraries or slow networks)
# INITIAL_BATCH_SIZE=100

# Execution Mode
# RUN_ONCE (optional): Set to "true" for one-shot execution. Omit or set to "false" for scheduled mode (default when unset).
# RUN_ONCE=true

# CRON_SCHEDULE: Required when RUN_ONCE is false or unset (scheduled mode)
# Examples: "0 9 * * *" (daily at 9 AM), "0 9 * * MON" (Mondays at 9 AM)
CRON_SCHEDULE=0 9 * * MON
