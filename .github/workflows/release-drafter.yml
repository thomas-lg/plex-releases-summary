name: Release Drafter

on:
    push:
        branches: [main]
    # pull_request_target runs in the context of the base repo (with write permissions),
    # even for PRs from forks. This is intentional: release-drafter needs write access
    # to update draft release notes. It is safe here because no PR code is checked out
    # or executed — only PR metadata (title, labels) is read by the action.
    pull_request_target:
        types: [opened, reopened, synchronize, edited]
    workflow_dispatch:
        inputs:
            dry_run:
                description: "Dry run (do not publish the release)"
                required: false
                default: "false"
                type: choice
                options:
                    - "false"
                    - "true"

jobs:
    draft-release:
        runs-on: ubuntu-latest
        if: github.event_name == 'push' || github.event_name == 'pull_request_target'
        permissions:
            contents: write
            # write is only needed if autolabeler is configured; read is sufficient here
            pull-requests: read

        steps:
            - name: Create/Update Draft Release
              uses: release-drafter/release-drafter@v6
              with:
                  config-name: release-drafter.yml
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    publish-release:
        runs-on: ubuntu-latest
        if: github.event_name == 'workflow_dispatch'
        permissions:
            contents: write
            pull-requests: read

        steps:
            - name: Publish Release
              uses: release-drafter/release-drafter@v6
              with:
                  config-name: release-drafter.yml
                  publish: ${{ github.event.inputs.dry_run == 'false' }}
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
